---
phase: 02-agent-task-reader
plan: 01
type: tdd
wave: 1
depends_on: ["01-01"]
files_modified:
  - wikify/maintenance/task_reader.py
  - wikify/cli.py
  - tests/test_maintenance_task_reader.py
  - tests/test_wikify_cli.py
  - README.md
  - LLM-Wiki-Cli-README.md
  - scripts/fokb_protocol.md
autonomous: true
requirements:
  - TSK-01
  - TSK-02
  - TSK-03
  - TSK-04
  - TSK-05
user_setup: []
must_haves:
  truths:
    - `wikify tasks` reads existing `sorted/graph-agent-tasks.json`.
    - `wikify tasks` supports `--status`, `--action`, `--id`, and `--limit`.
    - `wikify tasks --refresh` explicitly runs maintain before reading tasks.
    - Missing task queue artifact returns `agent_task_queue_missing`.
    - Task reading does not edit content pages or mutate task status.
  artifacts:
    - wikify/maintenance/task_reader.py
  key_links:
    - wikify.cli.cmd_tasks calls wikify.maintenance.task_reader.
    - wikify.cli.cmd_tasks calls wikify.maintenance.runner.run_maintenance only when `--refresh` is present.
---

<objective>
Build the `wikify tasks` read API for graph agent tasks.

Purpose: downstream agents need a stable command to list, filter, and inspect queued graph maintenance tasks without parsing artifact paths themselves.
Output: task reader module, CLI wiring, tests, docs, and GSD summary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@wikify/maintenance/task_queue.py
@wikify/maintenance/runner.py
@wikify/cli.py
@tests/test_wikify_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define task reader behavior with failing tests</name>
  <files>tests/test_maintenance_task_reader.py</files>
  <read_first>wikify/maintenance/task_queue.py</read_first>
  <action>Create `tests/test_maintenance_task_reader.py`. The tests should write a fake `sorted/graph-agent-tasks.json` into a temp KB, import `load_task_queue`, `select_tasks`, `TaskQueueNotFound`, and `TaskNotFound` from `wikify.maintenance.task_reader`, then assert loading works, filtering by status/action works, limiting works, selecting by id returns one task, missing queue raises `TaskQueueNotFound`, and missing id raises `TaskNotFound`.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_task_reader -v`; it must fail with `ModuleNotFoundError: No module named 'wikify.maintenance.task_reader'`.</verify>
  <acceptance_criteria>
    - Test file exists.
    - Failure is due to missing module, not syntax or fixture errors.
  </acceptance_criteria>
  <done>Red test defines the reader contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement task reader module</name>
  <files>wikify/maintenance/task_reader.py</files>
  <read_first>tests/test_maintenance_task_reader.py</read_first>
  <action>Create `wikify/maintenance/task_reader.py`. Implement `load_task_queue(base)`, `select_tasks(queue, status=None, action=None, task_id=None, limit=None)`, `TaskQueueNotFound`, and `TaskNotFound`. `load_task_queue` reads `sorted/graph-agent-tasks.json`. `select_tasks` returns schema `wikify.agent-task-selection.v1`, preserves source schema, returns selected tasks, `summary.task_count`, and `summary.total_task_count`. Negative limit raises `ValueError`. Missing id raises `TaskNotFound`.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_task_reader -v`; it must pass.</verify>
  <acceptance_criteria>
    - Reader loads the task queue artifact.
    - Reader filters by status/action/id and applies limit.
    - Missing artifacts and missing ids have typed exceptions.
  </acceptance_criteria>
  <done>Reader module passes focused tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire wikify tasks CLI</name>
  <files>wikify/cli.py, tests/test_wikify_cli.py</files>
  <read_first>wikify/cli.py, tests/test_wikify_cli.py</read_first>
  <action>Add parser support for `tasks` with `--status queued|in_progress|done|blocked`, `--action`, `--id`, `--limit`, `--refresh`, and `--policy conservative|balanced|aggressive` for refresh. Implement `cmd_tasks(args)`. Without `--refresh`, load the existing queue and select tasks. With `--refresh`, run `run_maintenance(discover_base(), policy=args.policy, dry_run=False)` first, then select from the returned task queue. Missing queue returns `envelope_error('tasks', 'agent_task_queue_missing', ..., 2, False, {'path': path})`. Missing id returns `agent_task_not_found` with exit code 2.</action>
  <verify>Run `python3 -m unittest tests.test_wikify_cli -v`; it must pass.</verify>
  <acceptance_criteria>
    - Parser accepts `tasks --status queued --limit 1`.
    - CLI can list tasks after `maintain`.
    - CLI can use `tasks --refresh`.
    - Missing artifact returns structured error.
  </acceptance_criteria>
  <done>`wikify tasks` is available as a stable JSON command.</done>
</task>

<task type="auto">
  <name>Task 4: Document wikify tasks</name>
  <files>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</files>
  <read_first>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</read_first>
  <action>Document `wikify tasks`, `--refresh`, `--status`, `--action`, `--id`, `--limit`, missing queue behavior, and V1 read-only behavior. Explain that task status mutation and patch application are future phases.</action>
  <verify>Run `rg -n "wikify tasks|agent_task_queue_missing|agent task reader" README.md LLM-Wiki-Cli-README.md scripts/fokb_protocol.md` and confirm all three docs mention the command or error.</verify>
  <acceptance_criteria>
    - README includes a short command example.
    - Product doc explains read-only consumer semantics.
    - Protocol doc lists command flags and errors.
  </acceptance_criteria>
  <done>Docs cover the agent task reader command.</done>
</task>

<task type="auto">
  <name>Task 5: Verify and summarize GSD phase</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/02-agent-task-reader/02-01-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Run full tests and smoke commands. Update requirements, roadmap, and state to mark Phase 2 complete. Create `02-01-SUMMARY.md` with commits, verification evidence, files changed, and next-phase readiness.</action>
  <verify>Run `python3 -m unittest discover -s tests -v`, run a temp KB smoke test for `wikify tasks --refresh`, `wikify tasks --status queued --limit 1`, and `wikify tasks --id agent-task-1`, then run `git status --short`.</verify>
  <acceptance_criteria>
    - Full tests pass.
    - Smoke test confirms refresh/list/id paths.
    - GSD state files reflect Phase 2 completion.
  </acceptance_criteria>
  <done>Phase 2 is complete, verified, summarized, and committed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_maintenance_task_reader -v`
- [ ] `python3 -m unittest tests.test_wikify_cli -v`
- [ ] `python3 -m unittest discover -s tests -v`
- [ ] Manual smoke test for `wikify tasks --refresh`, list filters, and id lookup.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `wikify tasks` is read-only unless `--refresh` is explicitly provided
- Missing queue and missing id return structured errors

</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-task-reader/02-01-SUMMARY.md`.
</output>
