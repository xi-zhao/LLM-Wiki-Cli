---
phase: 06-purpose-aware-proposals
plan: 01
type: tdd
wave: 1
depends_on: ["05-01"]
files_modified:
  - wikify/maintenance/purpose.py
  - wikify/maintenance/proposal.py
  - tests/test_maintenance_purpose.py
  - tests/test_maintenance_proposal.py
  - README.md
  - LLM-Wiki-Cli-README.md
  - scripts/fokb_protocol.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/06-purpose-aware-proposals/06-01-SUMMARY.md
autonomous: true
requirements:
  - PUR-01
  - PUR-02
  - PUR-03
  - PUR-04
user_setup: []
must_haves:
  truths:
    - Wikify discovers optional `purpose.md` or `wikify-purpose.md` at the wiki root.
    - `wikify propose` includes purpose context when present.
    - Missing purpose context is non-blocking and represented explicitly in proposal metadata.
    - Purpose context enriches proposal rationale only.
    - Purpose context never weakens write-scope/path validation.
  artifacts:
    - proposal.purpose_context
    - proposal.rationale
  key_links:
    - wikify.maintenance.proposal calls wikify.maintenance.purpose.
    - Path safety stays in wikify.maintenance.proposal before any artifact write.
---

<objective>
Make scoped patch proposals purpose-aware.

Purpose: agents should know why a proposed repair matters to the wiki's goals when the wiki declares those goals, while preserving all write-scope safety rules.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@wikify/maintenance/proposal.py
@tests/test_maintenance_proposal.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define purpose discovery with failing tests</name>
  <files>tests/test_maintenance_purpose.py</files>
  <read_first>wikify/maintenance/proposal.py</read_first>
  <action>Create tests for `load_purpose_context(base)`. Cover `purpose.md`, `wikify-purpose.md`, and missing purpose. Assert schema, present flag, relative path, title, excerpt, and non-blocking missing behavior.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_purpose -v`; it must fail with `ModuleNotFoundError`.</verify>
  <done>Red tests define purpose discovery contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement purpose context module</name>
  <files>wikify/maintenance/purpose.py</files>
  <read_first>tests/test_maintenance_purpose.py</read_first>
  <action>Create `wikify/maintenance/purpose.py` with stdlib-only Markdown extraction. Prefer `purpose.md` over `wikify-purpose.md`. Return `wikify.purpose-context.v1`, `present`, `path`, `relative_path`, `title`, `excerpt`, `goal_lines`, and `question_lines` when present. Missing purpose returns candidates and `present: false`.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_purpose -v`; it must pass.</verify>
  <done>Purpose context loads independently.</done>
</task>

<task type="auto">
  <name>Task 3: Attach purpose to patch proposals</name>
  <files>wikify/maintenance/proposal.py, tests/test_maintenance_proposal.py</files>
  <read_first>wikify/maintenance/proposal.py, tests/test_maintenance_proposal.py</read_first>
  <action>Update `build_patch_proposal` to include `purpose_context` and `rationale`. Purpose-present rationale should mention alignment with the purpose title/excerpt. Missing purpose should be explicitly non-blocking. Add a regression test proving out-of-scope path validation still fails when purpose exists.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_proposal -v`; it must pass.</verify>
  <done>Proposals are purpose-aware without weakening path safety.</done>
</task>

<task type="auto">
  <name>Task 4: Document purpose-aware proposals</name>
  <files>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</files>
  <read_first>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</read_first>
  <action>Document optional `purpose.md` / `wikify-purpose.md`, proposal metadata, missing-purpose behavior, and safety rule.</action>
  <verify>Run `rg -n "purpose.md|wikify-purpose|purpose_context|purpose-aware" README.md LLM-Wiki-Cli-README.md scripts/fokb_protocol.md` and confirm all three docs mention purpose behavior.</verify>
  <done>Docs cover purpose-aware proposals.</done>
</task>

<task type="auto">
  <name>Task 5: Verify and summarize GSD phase</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/06-purpose-aware-proposals/06-01-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Run full tests and temp-KB smoke with and without purpose. Update requirements, roadmap, and state to mark Phase 6 complete. Create summary with commits, verification evidence, and final milestone status.</action>
  <verify>Run `python3 -m unittest discover -s tests -v`; smoke `wikify propose` with purpose and without purpose; then run `git status --short`.</verify>
  <done>Phase 6 is complete, verified, summarized, and committed.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_maintenance_purpose -v`
- [ ] `python3 -m unittest tests.test_maintenance_proposal -v`
- [ ] `python3 -m unittest discover -s tests -v`
- [ ] Manual smoke for purpose-present and purpose-missing proposal paths.
</verification>

<success_criteria>

- Optional purpose context is discoverable.
- Proposals include purpose context and rationale.
- Missing purpose is non-blocking.
- Purpose context does not affect path/write-scope safety.
- No content pages are edited.

</success_criteria>
