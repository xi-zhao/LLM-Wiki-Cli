---
phase: 03-scoped-patch-proposal
plan: 01
type: tdd
wave: 1
depends_on: ["02-01"]
files_modified:
  - wikify/maintenance/proposal.py
  - wikify/cli.py
  - tests/test_maintenance_proposal.py
  - tests/test_wikify_cli.py
  - README.md
  - LLM-Wiki-Cli-README.md
  - scripts/fokb_protocol.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/03-scoped-patch-proposal/03-01-SUMMARY.md
autonomous: true
requirements:
  - PRP-01
  - PRP-02
  - PRP-03
  - PRP-04
  - PRP-05
  - PRP-06
user_setup: []
must_haves:
  truths:
    - `wikify propose --task-id <id>` reads exactly one existing graph agent task.
    - Proposal output is deterministic JSON and is written to `sorted/graph-patch-proposals/<task-id>.json`.
    - `--dry-run` returns the proposal but writes no proposal artifact.
    - Every proposed file path is validated against the selected task `write_scope`.
    - Proposal generation does not apply patches, rewrite content pages, or mutate task status.
    - Missing queue, missing task, missing write scope, and out-of-scope path cases return structured errors.
  artifacts:
    - wikify/maintenance/proposal.py
    - sorted/graph-patch-proposals/<task-id>.json
  key_links:
    - wikify.cli.cmd_propose calls wikify.maintenance.proposal.
    - wikify.maintenance.proposal reuses wikify.maintenance.task_reader for queue loading and task selection.
    - Proposal artifacts point back to source task id and finding id.
---

<objective>
Build the scoped patch proposal command for graph agent tasks.

Purpose: agents need a safe next step after `wikify tasks`: convert one queued maintenance task into an auditable proposal artifact that another agent or command can review later. This phase must not apply content edits.
Output: proposal module, CLI wiring, tests, docs, and GSD summary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/llm-wiki-reference.md
@wikify/maintenance/task_reader.py
@wikify/maintenance/task_queue.py
@wikify/cli.py
@tests/test_wikify_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define proposal behavior with failing tests</name>
  <files>tests/test_maintenance_proposal.py</files>
  <read_first>wikify/maintenance/task_reader.py, wikify/maintenance/task_queue.py</read_first>
  <action>Create `tests/test_maintenance_proposal.py`. Build a temp KB with `sorted/graph-agent-tasks.json`. Import `build_patch_proposal`, `write_patch_proposal`, `ProposalError`, and `OutOfScopeProposal` from `wikify.maintenance.proposal`. Assert that building a proposal for a task returns schema `wikify.patch-proposal.v1`, includes task id, source finding id, write scope, planned edits, acceptance checks, risk, and preflight. Assert missing write scope raises `ProposalError`. Assert a task proposing a path outside `write_scope` raises `OutOfScopeProposal`. Assert dry-run style build writes nothing.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_proposal -v`; it must fail with `ModuleNotFoundError: No module named 'wikify.maintenance.proposal'`.</verify>
  <acceptance_criteria>
    - Test file exists.
    - Failure is due to missing module, not syntax or fixture errors.
  </acceptance_criteria>
  <done>Red tests define the proposal contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement proposal module</name>
  <files>wikify/maintenance/proposal.py</files>
  <read_first>tests/test_maintenance_proposal.py, wikify/maintenance/task_reader.py</read_first>
  <action>Create `wikify/maintenance/proposal.py`. Implement `build_patch_proposal(base, task_id)`, `write_patch_proposal(base, proposal)`, `ProposalError`, and `OutOfScopeProposal`. Reuse `load_task_queue` and `select_tasks`. Derive proposal edits conservatively from existing task fields. Include no content diff generation yet unless the task evidence supports a deterministic path. The module must validate all proposed paths against task `write_scope` before returning success.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_proposal -v`; it must pass.</verify>
  <acceptance_criteria>
    - Proposal module builds deterministic JSON from one task.
    - Proposal writer creates `sorted/graph-patch-proposals/<task-id>.json`.
    - Out-of-scope paths are rejected before writing.
  </acceptance_criteria>
  <done>Proposal module passes focused tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire wikify propose CLI</name>
  <files>wikify/cli.py, tests/test_wikify_cli.py</files>
  <read_first>wikify/cli.py, tests/test_wikify_cli.py</read_first>
  <action>Add parser support for `propose --task-id <id> [--dry-run]`. Implement `cmd_propose(args)`. The command should build a proposal, write it unless `--dry-run` is set, and return a stable JSON envelope with `artifact_path`, `proposal`, and `summary`. Map missing queue to `agent_task_queue_missing`, missing id to `agent_task_not_found`, missing write scope to `proposal_write_scope_missing`, and out-of-scope path to `proposal_out_of_scope`, all with exit code 2.</action>
  <verify>Run `python3 -m unittest tests.test_wikify_cli -v`; it must pass.</verify>
  <acceptance_criteria>
    - Parser accepts `propose --task-id agent-task-1`.
    - CLI writes proposal artifact by default.
    - `--dry-run` writes no proposal artifact.
    - CLI returns structured errors for proposal failures.
  </acceptance_criteria>
  <done>`wikify propose` is available as a read-only proposal command.</done>
</task>

<task type="auto">
  <name>Task 4: Document proposal workflow</name>
  <files>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</files>
  <read_first>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</read_first>
  <action>Document the end-to-end agent loop: `wikify maintain`, `wikify tasks`, `wikify propose --task-id <id>`. Explain artifact path, dry-run behavior, write-scope validation, and the no-apply safety rule.</action>
  <verify>Run `rg -n "wikify propose|graph-patch-proposals|proposal_out_of_scope" README.md LLM-Wiki-Cli-README.md scripts/fokb_protocol.md` and confirm all three docs mention the proposal command or protocol.</verify>
  <acceptance_criteria>
    - README has a short command example.
    - Product doc explains why proposal precedes apply.
    - Protocol doc lists command flags and structured errors.
  </acceptance_criteria>
  <done>Docs cover scoped patch proposals.</done>
</task>

<task type="auto">
  <name>Task 5: Verify and summarize GSD phase</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/03-scoped-patch-proposal/03-01-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Run full tests and a temp-KB smoke test. Update requirements, roadmap, and state to mark Phase 3 complete. Create `03-01-SUMMARY.md` with commits, verification evidence, files changed, decisions, and Phase 4 readiness.</action>
  <verify>Run `python3 -m unittest discover -s tests -v`; run smoke commands for `wikify tasks --refresh`, `wikify propose --task-id agent-task-1`, and `wikify propose --task-id agent-task-1 --dry-run`; then run `git status --short`.</verify>
  <acceptance_criteria>
    - Full tests pass.
    - Smoke test confirms normal and dry-run proposal paths.
    - GSD state files reflect Phase 3 completion.
  </acceptance_criteria>
  <done>Phase 3 is complete, verified, summarized, and committed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_maintenance_proposal -v`
- [ ] `python3 -m unittest tests.test_wikify_cli -v`
- [ ] `python3 -m unittest discover -s tests -v`
- [ ] Manual smoke test for proposal artifact write and dry-run.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `wikify propose` never edits content pages
- Proposal paths are validated against task `write_scope`
- Structured errors are documented and covered by tests

</success_criteria>

<output>
After completion, create `.planning/phases/03-scoped-patch-proposal/03-01-SUMMARY.md`.
</output>
