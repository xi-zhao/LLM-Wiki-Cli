---
phase: 05-graph-relevance-scoring
plan: 01
type: tdd
wave: 1
depends_on: ["04-01"]
files_modified:
  - wikify/graph/relevance.py
  - wikify/graph/analytics.py
  - wikify/maintenance/findings.py
  - wikify/maintenance/task_queue.py
  - tests/test_graph_relevance.py
  - tests/test_graph_builder.py
  - tests/test_maintenance_findings.py
  - tests/test_maintenance_task_queue.py
  - README.md
  - LLM-Wiki-Cli-README.md
  - scripts/fokb_protocol.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/05-graph-relevance-scoring/05-01-SUMMARY.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
user_setup: []
must_haves:
  truths:
    - Graph relevance is deterministic and stdlib-only.
    - Signals include direct links, source overlap, common neighbors, and type affinity.
    - Relevance evidence is attached to graph analytics, findings, and graph agent tasks.
    - Relevance scores are advisory; they change explanation/priority context but do not apply writes.
    - Low-confidence relevance is informational and never escalates a task to high priority by itself.
  artifacts:
    - graph/graph.json analytics.relevance
    - sorted/graph-findings.json finding.relevance
    - sorted/graph-agent-tasks.json task.relevance
  key_links:
    - wikify.graph.analytics calls wikify.graph.relevance.
    - wikify.maintenance.findings copies subject relevance from analytics.
    - wikify.maintenance.task_queue carries finding relevance into tasks.
---

<objective>
Build graph relevance scoring for Wikify's graph maintenance loop.

Purpose: agents should see why a node or task is structurally relevant before proposing edits. This phase adds evidence-rich scores without making them an automatic write trigger.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/llm-wiki-reference.md
@wikify/graph/analytics.py
@wikify/graph/model.py
@wikify/maintenance/findings.py
@wikify/maintenance/task_queue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define relevance scoring with failing tests</name>
  <files>tests/test_graph_relevance.py</files>
  <read_first>wikify/graph/model.py, wikify/graph/analytics.py</read_first>
  <action>Create tests for `compute_relevance(nodes, edges)`. Use a small graph that exercises direct links, shared source paths, common neighbors, and type affinity. Assert schema `wikify.graph-relevance.v1`, signal-level evidence, confidence, pair ordering, and per-node `by_node` summaries.</action>
  <verify>Run `python3 -m unittest tests.test_graph_relevance -v`; it must fail with `ModuleNotFoundError`.</verify>
  <done>Red tests define relevance contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement graph relevance module and analytics integration</name>
  <files>wikify/graph/relevance.py, wikify/graph/analytics.py, tests/test_graph_builder.py</files>
  <read_first>tests/test_graph_relevance.py, wikify/graph/analytics.py</read_first>
  <action>Implement `compute_relevance(nodes, edges, limit=50, per_node_limit=5)`. Use direct extracted links, shared edge source_path overlap, Adamic-Adar-style common neighbors, and conservative type affinity. Add `relevance` to analytics. Update graph builder tests to assert analytics relevance exists.</action>
  <verify>Run `python3 -m unittest tests.test_graph_relevance tests.test_graph_builder -v`; it must pass.</verify>
  <done>Graph artifacts carry relevance analytics.</done>
</task>

<task type="auto">
  <name>Task 3: Attach relevance to findings and task queue</name>
  <files>wikify/maintenance/findings.py, wikify/maintenance/task_queue.py, tests/test_maintenance_findings.py, tests/test_maintenance_task_queue.py</files>
  <read_first>wikify/maintenance/findings.py, wikify/maintenance/task_queue.py</read_first>
  <action>When a finding subject has node relevance in analytics, attach a `relevance` object with max score/confidence and top related nodes. Copy finding relevance into graph agent tasks. Do not let low-confidence relevance raise task priority to high.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_findings tests.test_maintenance_task_queue -v`; it must pass.</verify>
  <done>Findings and tasks include advisory relevance metadata.</done>
</task>

<task type="auto">
  <name>Task 4: Document relevance scoring</name>
  <files>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</files>
  <read_first>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</read_first>
  <action>Document relevance signals, advisory behavior, and where metadata appears in graph/findings/tasks.</action>
  <verify>Run `rg -n "graph relevance|source overlap|common neighbors|type affinity" README.md LLM-Wiki-Cli-README.md scripts/fokb_protocol.md` and confirm all three docs mention relevance scoring.</verify>
  <done>Docs cover relevance scoring contract.</done>
</task>

<task type="auto">
  <name>Task 5: Verify and summarize GSD phase</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/05-graph-relevance-scoring/05-01-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Run full tests and temp-KB smoke. Update requirements, roadmap, and state to mark Phase 5 complete. Create summary with commits, verification evidence, files changed, decisions, and Phase 6 readiness.</action>
  <verify>Run `python3 -m unittest discover -s tests -v`; run smoke commands for `wikify graph --no-html` and `wikify maintain`; inspect graph/finding/task relevance; then run `git status --short`.</verify>
  <done>Phase 5 is complete, verified, summarized, and committed.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_graph_relevance -v`
- [ ] `python3 -m unittest tests.test_graph_builder tests.test_maintenance_findings tests.test_maintenance_task_queue -v`
- [ ] `python3 -m unittest discover -s tests -v`
- [ ] Manual smoke for graph/findings/tasks relevance metadata.
</verification>

<success_criteria>

- Relevance scores are deterministic and explainable.
- Direct links, source overlap, common neighbors, and type affinity are represented as signals.
- Graph analytics include relevance.
- Findings and tasks carry relevance metadata where applicable.
- Low-confidence relevance remains informational.
- No automatic content writes are introduced.

</success_criteria>
