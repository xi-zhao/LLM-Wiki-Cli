---
phase: 04-agent-task-lifecycle
plan: 01
type: tdd
wave: 1
depends_on: ["03-01"]
files_modified:
  - wikify/maintenance/task_lifecycle.py
  - wikify/cli.py
  - tests/test_maintenance_task_lifecycle.py
  - tests/test_wikify_cli.py
  - README.md
  - LLM-Wiki-Cli-README.md
  - scripts/fokb_protocol.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/04-agent-task-lifecycle/04-01-SUMMARY.md
autonomous: true
requirements:
  - LIF-01
  - LIF-02
  - LIF-03
  - LIF-04
  - LIF-05
user_setup: []
must_haves:
  truths:
    - `wikify tasks` remains read-only unless an explicit lifecycle action flag is present.
    - Lifecycle actions persist task status changes in `sorted/graph-agent-tasks.json`.
    - Lifecycle actions append audit events to `sorted/graph-agent-task-events.json`.
    - Invalid transitions return structured non-retryable errors.
    - Lifecycle commands never edit topic, parsed, source, sorted content pages, or proposal artifacts.
  artifacts:
    - wikify/maintenance/task_lifecycle.py
    - sorted/graph-agent-task-events.json
  key_links:
    - wikify.cli.cmd_tasks routes explicit lifecycle action flags to wikify.maintenance.task_lifecycle.
    - wikify.maintenance.task_lifecycle reuses wikify.maintenance.task_reader for queue loading and path resolution.
    - Proposal artifacts created by Phase 3 can be referenced by the `proposed` lifecycle event.
---

<objective>
Build durable graph agent task lifecycle transitions.

Purpose: once proposal artifacts exist, agents need a low-interruption way to mark task progress, retry failed work, cancel unsafe work, and restore blocked/rejected tasks while preserving an append-only audit trail.
Output: lifecycle module, explicit `wikify tasks` mutation flags, tests, docs, and GSD summary.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@wikify/maintenance/task_reader.py
@wikify/maintenance/proposal.py
@wikify/cli.py
@tests/test_wikify_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define lifecycle behavior with failing tests</name>
  <files>tests/test_maintenance_task_lifecycle.py</files>
  <read_first>wikify/maintenance/task_reader.py, tests/test_maintenance_task_reader.py</read_first>
  <action>Create focused tests for `apply_lifecycle_action(base, task_id, action, note=None, proposal_path=None)`. Cover queued -> proposed, proposed -> in_progress, in_progress -> done, failed -> queued retry, blocked -> queued restore, and queued -> rejected cancel. Assert queue status is persisted, event artifact is appended, event ids are stable increments, and invalid transitions raise `InvalidTaskTransition`.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_task_lifecycle -v`; it must fail with `ModuleNotFoundError: No module named 'wikify.maintenance.task_lifecycle'`.</verify>
  <done>Red tests define lifecycle contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lifecycle module</name>
  <files>wikify/maintenance/task_lifecycle.py</files>
  <read_first>tests/test_maintenance_task_lifecycle.py, wikify/maintenance/task_reader.py</read_first>
  <action>Create lifecycle module with finite transition map, queue writer, event log writer, `apply_lifecycle_action`, `InvalidTaskTransition`, and `TaskLifecycleError`. Preserve existing task fields, add `updated_at`, `status_changed_at`, optional `proposal_path`, and increment `attempts` on retry. Event schema should be `wikify.graph-agent-task-events.v1`.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_task_lifecycle -v`; it must pass.</verify>
  <done>Lifecycle module passes focused tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire explicit lifecycle actions into wikify tasks</name>
  <files>wikify/cli.py, tests/test_wikify_cli.py</files>
  <read_first>wikify/cli.py, tests/test_wikify_cli.py</read_first>
  <action>Add explicit lifecycle action flags to `tasks`: `--mark-proposed`, `--start`, `--mark-done`, `--mark-failed`, `--block`, `--cancel`, `--retry`, `--restore`, plus optional `--note` and `--proposal-path`. Require `--id` when an action flag is used. Without action flags, keep existing read-only behavior unchanged. Return schema `wikify.agent-task-lifecycle.v1` for lifecycle actions and map invalid transitions to `invalid_agent_task_transition` exit code 2.</action>
  <verify>Run `python3 -m unittest tests.test_wikify_cli -v`; it must pass.</verify>
  <done>CLI lifecycle actions work and default task reads stay backward compatible.</done>
</task>

<task type="auto">
  <name>Task 4: Document lifecycle workflow</name>
  <files>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</files>
  <read_first>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</read_first>
  <action>Document the loop: `maintain` -> `tasks` -> `propose` -> `tasks --mark-proposed` -> `tasks --start` -> `tasks --mark-done`. Explain event artifact, retry/cancel/restore, invalid transition errors, and read-only default.</action>
  <verify>Run `rg -n "mark-proposed|graph-agent-task-events|invalid_agent_task_transition" README.md LLM-Wiki-Cli-README.md scripts/fokb_protocol.md` and confirm all three docs mention lifecycle behavior.</verify>
  <done>Docs cover task lifecycle.</done>
</task>

<task type="auto">
  <name>Task 5: Verify and summarize GSD phase</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/04-agent-task-lifecycle/04-01-SUMMARY.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Run full tests and temp-KB smoke. Update requirements, roadmap, and state to mark Phase 4 complete. Create `04-01-SUMMARY.md` with commits, verification evidence, files changed, decisions, and Phase 5 readiness.</action>
  <verify>Run `python3 -m unittest discover -s tests -v`; run smoke commands for refresh, propose, mark-proposed, start, mark-done, retry/restore/cancel examples on synthetic queues as needed; then run `git status --short`.</verify>
  <done>Phase 4 is complete, verified, summarized, and committed.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_maintenance_task_lifecycle -v`
- [ ] `python3 -m unittest tests.test_wikify_cli -v`
- [ ] `python3 -m unittest discover -s tests -v`
- [ ] Manual smoke for lifecycle actions and event artifact.
</verification>

<success_criteria>

- Lifecycle actions are explicit and auditable.
- Default `wikify tasks` remains read-only.
- Queue status changes persist only when lifecycle action flags are used.
- Every transition appends an event.
- Invalid transitions are structured errors.
- No content pages are edited.

</success_criteria>
