---
phase: 01-graph-agent-task-queue
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - wikify/maintenance/task_queue.py
  - wikify/maintenance/runner.py
  - tests/test_maintenance_task_queue.py
  - tests/test_maintenance_runner.py
  - README.md
  - LLM-Wiki-Cli-README.md
  - scripts/fokb_protocol.md
autonomous: true
requirements:
  - GMT-01
  - GMT-02
  - GMT-03
  - GMT-04
  - GMT-05
  - DOC-01
  - DOC-02
user_setup: []
must_haves:
  truths:
    - Agent tasks are generated only from queued maintenance execution results.
    - Dry-run returns task previews but does not write sorted/graph-agent-tasks.json.
    - Normal maintain writes sorted/graph-agent-tasks.json.
    - No content pages are modified by task queue generation.
  artifacts:
    - wikify/maintenance/task_queue.py
    - sorted/graph-agent-tasks.json from a normal maintain run
  key_links:
    - wikify.maintenance.runner.run_maintenance calls wikify.maintenance.task_queue.build_task_queue.
    - CLI result artifacts include the task queue path.
---

<objective>
Build the graph agent task queue artifact for `wikify maintain`.

Purpose: queued graph maintenance findings should become deterministic, agent-consumable work packets without prompting the user or embedding LLM calls in the CLI.
Output: `wikify/maintenance/task_queue.py`, runner integration, tests, docs, and GSD summary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@wikify/maintenance/runner.py
@wikify/maintenance/planner.py
@wikify/maintenance/executor.py
@wikify/maintenance/findings.py
@tests/test_maintenance_runner.py
@README.md
@LLM-Wiki-Cli-README.md
@scripts/fokb_protocol.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define task queue contract with failing tests</name>
  <files>tests/test_maintenance_task_queue.py</files>
  <read_first>wikify/maintenance/planner.py, wikify/maintenance/executor.py</read_first>
  <action>Create `tests/test_maintenance_task_queue.py`. The test imports `build_task_queue` from `wikify.maintenance.task_queue`, builds a plan with one queued `queue_link_repair` step and one executed `record_graph_health_snapshot` step, applies the plan, and asserts only the queued step becomes a task. The expected document schema is `wikify.graph-agent-tasks.v1`. Each task must contain `id`, `source_finding_id`, `action`, `priority`, `target`, `evidence`, `write_scope`, `agent_instructions`, `acceptance_checks`, `requires_user`, and `status`.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_task_queue -v`; it must fail with `ModuleNotFoundError: No module named 'wikify.maintenance.task_queue'`.</verify>
  <acceptance_criteria>
    - `tests/test_maintenance_task_queue.py` exists.
    - Red test fails because the module does not exist, not because of syntax errors.
  </acceptance_criteria>
  <done>Failing test proves the new task queue contract is not implemented yet.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic task queue builder</name>
  <files>wikify/maintenance/task_queue.py</files>
  <read_first>tests/test_maintenance_task_queue.py, wikify/maintenance/executor.py</read_first>
  <action>Create `wikify/maintenance/task_queue.py` with `build_task_queue(plan: dict, execution: dict, findings: list[dict]) -> dict`. It should build tasks only for execution results with `status == 'queued'`. Map finding ids to findings for evidence. Use priority `high` for warning/critical severities and `normal` for info. Use `write_scope` as a list containing the finding subject when present, otherwise an empty list. Use action-specific instructions and acceptance checks for `queue_link_repair`, `queue_orphan_attachment`, `queue_digest_refresh`, and `queue_community_synthesis`. Set `requires_user` to `False` and `status` to `queued`.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_task_queue -v`; it must pass.</verify>
  <acceptance_criteria>
    - `wikify/maintenance/task_queue.py` exports `build_task_queue`.
    - The document contains `schema_version`, `generated_at`, `summary`, and `tasks`.
    - Only queued execution results become tasks.
  </acceptance_criteria>
  <done>Task queue builder passes its focused unit test.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate task queue into maintain runner</name>
  <files>wikify/maintenance/runner.py, tests/test_maintenance_runner.py</files>
  <read_first>wikify/maintenance/runner.py, tests/test_maintenance_runner.py</read_first>
  <action>Extend runner tests to assert normal `run_maintenance` writes `sorted/graph-agent-tasks.json` and dry-run does not. Update `run_maintenance` to call `build_task_queue(plan, execution, findings)`, include it in the returned result under `task_queue`, include `task_count` in `summary`, add `agent_tasks` to `artifacts`, and write the JSON artifact only when not dry-run.</action>
  <verify>Run `python3 -m unittest tests.test_maintenance_runner tests.test_wikify_cli -v`; it must pass.</verify>
  <acceptance_criteria>
    - Normal maintain writes `sorted/graph-agent-tasks.json`.
    - Dry-run returns task preview in result but writes no task queue artifact.
    - Existing CLI dry-run behavior remains green.
  </acceptance_criteria>
  <done>Runner integration works without regressing CLI tests.</done>
</task>

<task type="auto">
  <name>Task 4: Document agent task queue protocol</name>
  <files>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</files>
  <read_first>README.md, LLM-Wiki-Cli-README.md, scripts/fokb_protocol.md</read_first>
  <action>Document `sorted/graph-agent-tasks.json`, the schema purpose, dry-run behavior, and V1 safety rule. State clearly that this artifact is for later agents to consume and that `wikify maintain` still does not edit content pages or call hidden LLMs.</action>
  <verify>Run `rg -n "graph-agent-tasks|agent task" README.md LLM-Wiki-Cli-README.md scripts/fokb_protocol.md` and confirm all three docs mention the artifact.</verify>
  <acceptance_criteria>
    - README mentions `sorted/graph-agent-tasks.json`.
    - Product doc explains downstream agent consumption.
    - Protocol doc lists artifact and safety behavior.
  </acceptance_criteria>
  <done>Docs describe the task queue contract for users and automation.</done>
</task>

<task type="auto">
  <name>Task 5: Verify and summarize GSD phase</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/01-graph-agent-task-queue/01-01-SUMMARY.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</read_first>
  <action>Run the full unit test suite and a maintain smoke test. Update GSD roadmap, requirements, and state to mark Phase 1 complete. Create `01-01-SUMMARY.md` with commits, files changed, verification evidence, decisions, and next-phase readiness.</action>
  <verify>Run `python3 -m unittest discover -s tests -v`, run a temp KB smoke test for `maintain --dry-run` and `maintain`, and run `git status --short`.</verify>
  <acceptance_criteria>
    - Full tests pass.
    - Smoke test confirms dry-run does not write task artifact and normal run does.
    - GSD state files reflect Phase 1 completion.
  </acceptance_criteria>
  <done>Phase 1 is complete, verified, summarized, and committed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_maintenance_task_queue -v`
- [ ] `python3 -m unittest tests.test_maintenance_runner tests.test_wikify_cli -v`
- [ ] `python3 -m unittest discover -s tests -v`
- [ ] Manual maintain smoke test confirms dry-run and normal artifact behavior.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No content-page writes are introduced
- `sorted/graph-agent-tasks.json` exists after a normal maintain run
- `sorted/graph-agent-tasks.json` does not exist after a dry-run

</success_criteria>

<output>
After completion, create `.planning/phases/01-graph-agent-task-queue/01-01-SUMMARY.md`.
</output>
